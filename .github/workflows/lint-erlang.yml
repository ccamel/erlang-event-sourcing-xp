name: Lint / Erlang

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/lint-erlang.yml'
      - 'apps/**'
      - 'rebar.config'
      - 'rebar.lock'
      - 'elvis.config'

  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/lint-erlang.yml'
      - 'apps/**'
      - 'rebar.config'
      - 'rebar.lock'
      - 'elvis.config'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-erlang:
    runs-on: ubuntu-22.04
    env:
      ERL_FLAGS: "-enable-feature all"
      OTP_VERSION: "27"
      REBAR3_VERSION: "3.24.0"
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Setup Erlang/OTP and rebar3
        uses: erlef/setup-beam@v1
        with:
          otp-version: ${{ env.OTP_VERSION }}
          rebar3-version: ${{ env.REBAR3_VERSION }}

      - name: Restore _build folder
        uses: actions/cache@v6
        with:
          path: _build
          key: "_build-cache-for\
            -otp-${{ env.OTP_VERSION }}\
            -rebar3-${{ env.REBAR3_VERSION }}\
            -hash-${{ hashFiles('rebar.lock') }}"

      - name: Restore rebar3's cache
        uses: actions/cache@v6
        with:
          path: ~/.cache/rebar3
          key: "rebar3-cache-for\
            -otp-${{ env.OTP_VERSION }}\
            -rebar3-${{ env.REBAR3_VERSION }}\
            -hash-${{ hashFiles('rebar.lock') }}"

      - name: Lint Erlang code
        run: rebar3 lint

      - name: Update dialyzer PLT
        run: rebar3 dialyzer --update-plt || true

      - name: Run dialyzer
        run: rebar3 dialyzer

      - name: Ensure code is formatted
        run: rebar3 fmt --check

      - name: Ensure documentation generates
        run: rebar3 ex_doc
